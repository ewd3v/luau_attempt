return {
	analyzePaths = { "src", "tests", "scripts" }, -- Paths that should have their .luau files analyzed / linted / formatting checked
	buildRuntimes = { "lune", "roblox" }, -- Runtimes to build and publish for
	testRuntimes = { "lune" }, -- Runtimes to run tests on
}
